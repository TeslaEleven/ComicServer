<head>
<link rel="manifest" href="/manifest.json">
<link rel="apple-touch-icon" href="https://old.comicserver.org/img/core.png">
</head>
<div id="birthdaytheme">
<p onclick="window.location='/'" style="font-size:3em;cursor:pointer;text-align:center;font-family:Lobster;color:var(--ba);margin:0" id="nameplate">Comicserver</p>
<div style="border-radius:5px;background-image: linear-gradient(to right top, pink, purple);-webkit-background-clip: text;-webkit-text-fill-color: transparent;margin-left:auto;margin-right:auto;width:37.5%;cursor:pointer" id="cdwn" onclick="window.location.replace('./explore');"><p style="padding-left:10px;text-align:center"><i class="bi bi-compass"></i> Try Explore!</p></div>
<div class="main" id="cents" style="display:none">
<span class="main">
  <select id="selectBoxes" style="width:100%;-webkit-appearance:none" onchange="changeName();" class="hello">
    <option value="future">Search for a Future Comic</option>
    <option value="search">Search by Query</option>
    <option value="name" selected>Search by Name</option>
    <option value="ai">Make an AI Comic</option>
  </select>
</span>
<script>
      if(window.location.pathname === "/explore") {
document.getElementById("cdwn").style.display = "none"
      }
      if(localStorage.getItem('sb') === "true") {
      document.getElementById('cents').style.display = "block"
      }
      </script>
<form style="display:none" class="cents" method="post" onsubmit="return submitcheck(this);" id="ai">
        <input class="input" style="font-size:3vw" type="text" name="formulae" id="aigen" placeholder="Start with a Prompt" required>
        <input type="submit" name="submitbutton" value="Go" onclick="flick(thingyyy);">
      </form>
    <form class="cents" method="post" onsubmit="return submitcheck(this);" id="name" name="selected">
        <input class="input" style="font-size:3vw" type="text" name="thingyyys" id="formulae" placeholder="Start with a Comic" required>
        <input type="submit" name="submitbutton" value="Go" onclick="eklik(thingyyys);">
      </form>
      <script>
      function eklik(element)
      {
      if(element.value === ""){
      return;
      }
      if(element.value === " "){
      return;
      }
      if(element.value === "‎"){
      return;
      }
        var urlstring = `/load?name=${element.value}`;
        window.location.replace(urlstring);
      }
      </script>
    <form style="display:none" class="cents" method="post" onsubmit="return submitcheck(this);" id="future">
        <input class="input" style="font-size:3vw" type="text" name="thingyyy" id="er" placeholder="Start with a Name" required>
        <input type="submit" name="submitbutton" value="Go" onclick="klik(thingyyy);">
      </form>
      <script>
      function klik(element)
      {
      if(element.value === ""){
      return;
      }
      if(element.value === " "){
      return;
      }
      if(element.value === "‎"){
      return;
      }
        var urlstring = `/tomorrow?name=${element.value}`;
        window.location.replace(urlstring);
      }
      </script>
          <form style="display:none" class="cents" method="post" onsubmit="return submitcheck(this);" name="viewgen" id="search">        <input class="input" style="font-size:3vw" type="text" name="thingyy" id="ph" placeholder="Start with a Query" required>
        <input type="submit" name="submitbutton" value="Go" onclick="elik(thingyy);">
      </form>
      <script>
      function elik(element)
      {
      if(element.value === ""){
      return;
      }
      if(element.value === " "){
      return;
      }
      if(element.value === "‎"){
      return;
      }
        var urlstring = `/search?name=${element.value}`;
        window.location.replace(urlstring);
      }
      function submitcheck(element)
      {
        return false;
      }
function changeName() {
var selectBox = document.getElementById("selectBoxes");
var selectedValue = selectBox.options[selectBox.selectedIndex].value;
if(selectedValue === "explore") {
window.location.replace('./explore')
} else {
document.getElementById(selectedValue).style.display = 'block'
document.getElementsByName('selected')[0].style.display = 'none'
document.getElementsByName('selected')[0].name = null
document.getElementById(selectedValue).name = "selected"
}
}
</script>
<script>

if( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) ) {
document.getElementById('cdwn').style.width = "100%"
document.getElementById('birthdaytheme').style.display = "hidden"
document.getElementById('cents').style.width = "100%"
document.getElementById('cents').style.maxWidth = "80%"
document.getElementById('cents').style.width = "100%"
document.getElementById('cents').style.maxWidth = "85%"
document.getElementById('cents').style.width = "100%"
//document.getElementById('cents').style.display = "none"
document.getElementById('aigen').style.fontSize = "6.5vw"
document.getElementById('formulae').style.fontSize = "7vw"
document.getElementById('er').style.fontSize = "7vw"
document.getElementById('ph').style.fontSize = "7vw"
}
if(localStorage.getItem('theme') === 'sketch') {
document.getElementById('nameplate').style.fontFamily = "Chicago";
document.getElementById('nameplate').innerText = "ComicServer";
}  
if(localStorage.getItem('theme') === 'dispatch') {
document.getElementById('nameplate').style.fontFamily = "Emblema One"
}
</script>
  <button class="mbutton" id="prev" onclick="prevStrip();" style="display:none">Previous</button>
  <button id="random" class="mbutton" onclick="shufStrip();" style="display:none">Shu-ffle</button>
  <button id="next" class="mbutton" onclick="nextStrip();" style="display:none">Next</button>
</div>
<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
.sticky {
  position: fixed;
  top: 0;
  width: 100%;
  height: 75%;
}
</style>
<link rel="shortcut icon" href="https://old.comicserver.org/img/core.png" />
</head>
<body>
</body>
</html>
<%- include('theme') %>